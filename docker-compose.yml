services:
  backend:
    build:
      context: ./backend
      dockerfile: dockerfile
    ports:
      - "5000:5000"
    volumes:
      - ./backend:/usr/src/app
      - /usr/src/app/node_modules
    env_file:
      - .env
        # Add this section to connect to your local database
    extra_hosts:
      - "host.docker.internal:host-gateway"
    # depends_on:
    #   - db

  # db:
  #   image: mysql:8.0
  #   ports:
  #     - "3306:3306"
  #   env_file:
  #     - .env
  #   volumes:
  #     - db_data:/var/lib/mysql
  
  # phpmyadmin:
  #   image: phpmyadmin/phpmyadmin
  #   depends_on:
  #     - db
  #   ports:
  #     - "8080:80"
  #   environment:
  #     PMA_HOST: db
  #     MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}

# volumes:
#   db_data:
